import type { Viewport } from 'next'

export default {
  minimumScale: 1,
  initialScale: 1,
  width: 'device-width',
  userScalable: false,
  viewportFit: 'cover',
  themeColor: '#0F0F12',
  colorScheme: 'dark light',
} as Viewport
